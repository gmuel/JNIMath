DEFINE QLOCAL(PREC_REQ_BO) REPLACE DESCR('Precision Service Request BO Q') +
    PUT(ENABLED) GET(ENABLED) SHARE
DEFINE QLOCAL(PREC_REQ) REPLACE DESCR('Precision Service Request Q') +
    PUT(ENABLED) BOQNAME('PREC_REQ_BO') GET(ENABLED) SHARE

DEFINE QLOCAL(PREC_RESP) REPLACE DESCR('Precision Service Request Q') +
    PUT(ENABLED) BOQNAME('PREC_REQ_BO') GET(ENABLED) SHARE
